{
  "name": "obby",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "scripts": {
    "dev": "pnpm --filter @obby/web dev",
    "build": "pnpm --filter @obby/web build",
    "preview": "pnpm --filter @obby/web preview",
    "typecheck": "pnpm --filter @obby/web exec tsc -b",
    "test": "pnpm --filter @obby/web test"
  }
}
